extends ../layout

block contenido
    h1 Editar Proveedor: #{proveedor.tipoDoc === 'DNI' ? proveedor.nombre : proveedor.razonSocial}

    form(action=`/proveedores/editar/${proveedor.id}` method="POST")
        div
            label ID:
            input(type="number" value=proveedor.id disabled)

        div
            label(for="tipoDoc") Tipo de Documento:
            select(id="tipoDoc" name="tipoDoc")
                option(value="DNI" selected=proveedor.tipoDoc === "DNI") DNI
                option(value="CUIT" selected=proveedor.tipoDoc === "CUIT") CUIT

        div#campo-nombre
            label(for="nombre") Nombre:
            input(type="text" id="nombre" name="nombre" value=proveedor.nombre)

        div#campo-razonSocial
            label(for="razonSocial") Razón Social:
            input(type="text" id="razonSocial" name="razonSocial" value=proveedor.razonSocial)

        div
            label(for="nroDoc") Número de Documento:
            input(type="text" id="nroDoc" name="nroDoc" required value=proveedor.nroDoc)

        div
            label(for="email") Email:
            input(type="email" id="email" name="email" value=proveedor.email)

        div
            label(for="telefono") Teléfono:
            input(type="text" id="telefono" name="telefono" value=proveedor.telefono)

        div
            label(for="direccion") Dirección:
            input(type="text" id="direccion" name="direccion" value=proveedor.direccion)

        div
            label(for="saldoCuentaCorriente") Saldo de Cuenta Corriente ($):
            input(type="number" id="saldoCuentaCorriente" name="saldoCuentaCorriente" step="0.01" value=proveedor.saldoCuentaCorriente required)
            
        div.form-actions
            button(type="submit").btn-guardar Guardar Cambios
            a(href="/proveedores/listar").btn-cancelar Cancelar

    script.
        const select = document.getElementById('tipoDoc');
        const campoNombre = document.getElementById('campo-nombre');
        const campoRazon = document.getElementById('campo-razonSocial');
        const inputNombre = document.getElementById('nombre');
        const inputRazon = document.getElementById('razonSocial');

        function actualizarCampos() {
            if (select.value === 'CUIT') {
                campoNombre.style.display = 'none';
                campoRazon.style.display = 'block';
                inputNombre.removeAttribute('required');
                inputRazon.setAttribute('required', 'true');
            } else {
                campoNombre.style.display = 'block';
                campoRazon.style.display = 'none';
                inputRazon.removeAttribute('required');
                inputNombre.setAttribute('required', 'true');
            }
        }

        select.addEventListener('change', actualizarCampos);
        actualizarCampos();
